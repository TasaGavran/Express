{
  "name": "express-restoran-jagodina",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "live": "vite build && npx --yes live-server dist --port=5500 --open=/index.html"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
